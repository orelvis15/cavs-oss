[plugin]

name="CAVS"
description="Content-addressable, verified pack delivery. Downloads only the chunks that changed between versions, reconstructs the PCK byte-for-byte, verifies it, and mounts it at runtime with load_resource_pack()."
author="Orelvis Lago Vasallo"
version="0.2.0"
script="plugin.gd"
